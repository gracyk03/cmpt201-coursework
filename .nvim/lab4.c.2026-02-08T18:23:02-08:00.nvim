#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <stdint.h>

struct node {
  unit64_t size;
  struct node *next;
};

int main(void) {
  void *header = sbrk(0);
  printf("first program break address: %p\n", header);
}
